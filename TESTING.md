# Verification map

## Current release verification — September 13, 2026

The agreed desktop release verification is complete; owner review and portal submission remain. [STEWARD-RESPONSE.md](STEWARD-RESPONSE.md) maps the nine supplied requirements to immutable source, exact live inputs/outputs and public access evidence. [RELEASE-CHECKLIST.md](RELEASE-CHECKLIST.md) distinguishes current gates from broader untested paths.

The complete clean [Ubuntu suite on release commit 25376e8](https://github.com/sanity456/reply-check/actions/runs/34758798061) passed: 194 frontend tests, 97 contract/tooling tests, all 20 registered offline replays, formatting, lint, types, the production build and dependency audits. Test counts are checkpoint-specific; later documentation consistency tests add coverage without recreating live or human observations. Run the entire suite for the commit under review, not just the affected test.

The final [public-origin human-wallet record](evidence/release/20260913-public-wallet-reconciled.json) proves one finalized review (16 to 17), stored at chain timestamp `2026-09-13T12:59:48.716658+00:00`, with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`. Receipt, exact calldata, decoded return and full stored state agree. [Anonymous access evidence](evidence/release/20260913-evaluator-link-audit.json) covers the public example and immutable evidence links. No new wallet action is implied by this documentation update.

Historical reports below preserve their original failures, open cases and test totals. Their statements that publication, public CI or later wallet/accessibility cases remain pending describe those earlier checkpoints only. Current application/runtime pins remain strict; [exact previous report bytes](evidence/source-snapshots/20260913-before-release-clarification/document-bytes.json) resolve historical document hashes without substituting new text into old evidence. Full accessibility certification, every live race/adversarial path and physical-phone testing are not claimed.

## Historical checkpoints and test procedures

Latest native listening checkpoint (September 13): the human confirmed Narrator read the question's label/error guidance, reply-error guidance, and confirmation-dialog title/consent label. Correction and safe unsigned cancellation also passed DOM/UI checks. Narrator is off again and the original public fixture/latest review with 16 rows is restored. See [scoped speech evidence](evidence/browser/20260913-native-screen-reader-closure.json). No audio recording, complete screen-reader matrix or exact “invalid” announcement is claimed. Application/test/contract/lock files are unchanged; the prior 280-test and 20-replay results were not rerun for these documentation/evidence-only checks. Physical devices, other engines and public release gates remain open; earlier speech-pending records below are historical.

Latest UI fix (September 13): [A11Y-05 field feedback](evidence/local/20260913-draft-field-feedback-fix.json) is implemented and retested on the rebuilt preview. The complete existing Windows suites pass **280 tests (183 frontend, 97 contract/tooling)**, plus 20 registered evidence replays, lint, formatting, types and production build. Sixteen scoped Chrome checks across 19 observations verify field errors, limits, correction and restored history. This is not a clean Ubuntu run or complete accessibility certification. Older checkpoints below retain their original source and counts.

Latest accessibility/reliability checkpoint (September 13): [actual human-set 200% Chrome zoom](evidence/browser/20260913-native-accessibility-reliability.json) passed the scoped five-screen/two-dialog checks and was restored to 100%. The full 267-test local suite, 20 registered evidence replays and static checks passed again without source changes. Safe missing-review feedback, retry and navigation preservation passed in the live preview. [A11Y-05](UI-AUDIT.md) recommends directly associated draft-field errors. This is not public Ubuntu CI or complete submission approval.

The subsequent native preference follow-up passed [12 reduced-motion checks](evidence/browser/20260913-native-reduced-motion.json) and [11 Aquatic contrast/restoration checks](evidence/browser/20260913-native-contrast-restoration.json). Chrome detected the real Windows preferences; control/dialog transitions and the visible loading spinner respected reduced motion. Five screens and two dialogs remained usable in high contrast. Animation effects On, contrast theme None and 100% zoom were restored, with 16 loaded reviews and no request pending. Source/evidence pins remain unchanged; the full suite was not rerun for these documentation/evidence-only follow-ups. Actual screen-reader speech, physical devices, other engines and consolidated offline/CI coverage of the new records remain open.

Latest live checkpoint: [native hashless recovery passed](evidence/human-wallet/20260913-native-hash-recovery-reconciled.json). The real request survived an app reload, missing and unrelated hashes were rejected safely, and the correct human-provided hash attached through the real UI. Exact receipt/call/return/source/full-state checks prove one 15-to-16 review delta. The [closing browser check](evidence/human-wallet/20260913-native-hash-recovery-browser-close.json) follows independent verification. The prior wording-only checkpoint below is historical; its separate native case is now closed. Legacy already-persisted wrong-hash replacement and the broader live/accessibility/release matrix are not claimed complete.

## Offline tests

The offline suite currently contains **97 contract/tooling tests** and **183 frontend/transport tests (280 total)**. Both complete suites were rerun successfully for the field-feedback fix, along with all 20 registered saved-evidence replays. These were local Windows runs using the existing locked environments, not a fresh Ubuntu install or public Actions run. Test counts can change; trust the command output for a particular commit. Historical evidence retains the test count at its original checkpoint.

`tests/web/draft-feedback.test.mjs` evaluates the real component's validation expressions and editor JSX, exercising actual change/blur callbacks with isolated state setters. It covers UTF-8 and sentence boundaries, touched state, privacy/control checks, associated live errors, correction, escaped markup and unchanged permission/submission gates. `tests/web/draft-ui-history.test.mjs` requires exact pre-edit UI bytes for explicitly historical replays and rejects arbitrary stale-source substitution; AST comparisons confirm the submission, selection, lookup and busy/identity guards are unchanged. These tests do not hear screen-reader speech or execute a wallet. The initial focused run had seven harness failures because its isolated CommonJS context lacked `exports`; after that test-only setup correction all 13 focused tests passed. No application regression is claimed from that harness error.

`tests/web/missing-transaction.test.mjs` exercises the actual pinned RPC client against isolated JSON-RPC error responses and the actual recovery/status callbacks against storage/lock doubles. It checks exact original journal bytes, retry availability, nonterminal behavior, an exactly verified replacement, and rejection of misleading resource/network error shapes. A separate explicit read-only Studionet lookup confirms the production error shape and message. See [wording fix evidence](evidence/local/20260913-missing-hash-wording-fix.json). No wallet request was made; native-browser missing-hash recovery remains a separate open case.

- `tests/direct`: permissions, role acceptance/revocation, two-step ownership, archiving, limits, stored chain time, immutable versions, idempotency, reference freshness, model schemas, sentence coverage, citation integrity, answer-card eligibility and retirement.
- `tests/consensus`: exercises the contract’s actual validator callback with adversarial fixtures. It checks independent assessment, semantic-audit rejection, altered derived fields, forged quotes, misleading rewrites and failures. This is **not real multi-node consensus**.
- `tests/web`: input and privacy limits, role matrix, canonical digests, source-hash pin, wallet request/rejection behavior, network switching, strict receipt identity and execution, calldata binding with the installed SDK, recovery metadata, form submission wiring and optional WebMCP contracts.

Direct-mode model responses are mocked. No offline test should be cited as proof of live LLM reliability. The contract uses the deterministic GenVM message timestamp; direct tests warp that context and compare the exact stored value. There is no deadline or escrow timer in ReplyCheck.

## Current local environment

- Windows; Node 24.18.0; Python 3.12.14.
- Fresh `npm ci` completed successfully.
- A new `.verify-venv` was installed from `requirements.lock` with `--require-hashes`; all 97 contract/tooling tests passed there.
- JavaScript and Python dependency audits: no reported vulnerabilities at the time of the local runs.
- Lint, TypeScript and formatting pass for application-owned files. The production export completed successfully after the documented Windows shutdown workaround.
- This machine has no WSL installation. A public Ubuntu Actions run has **not** occurred.

On September 12, disabling OneDrive Documents backup relocated the project to `C:/Users/user/Documents/genlayer/reply-check`. The incomplete JavaScript and Python environments were preserved outside the project and rebuilt from the unchanged locks. Twelve original evidence/fixture files left in the old folder were restored with exact SHA-256 matches. A linter import-resolution failure reproduced on the relocated cloud-marked files but not on an identical plain copy; restoring ordinary local copies with every byte hash unchanged resolved it. Original files remain recoverable in the recorded temporary preservation folders. See [relocation verification](evidence/local/20260912-relocation-verification.json) for the current checks and scope. No application or contract logic changed during this recovery.

## Local UI layout pass

The purple/gold local preview was checked in the in-app browser at a 1365 × 900 desktop viewport and 390px/320px phone widths. The narrow layout has no document-level horizontal overflow; all five navigation tabs remain visible.

Checked: typing and byte/sentence counters, preserving a draft across tab changes, keyboard tab navigation and activation, workspace-dialog dismissal with Escape, disabled live-review controls, and incorrect-answer feedback/retry in Reply Gym. The test draft was cleared and saved practice progress was left unchanged. No browser console errors were reported during this pass.

These checks cover the undeployed preview, not populated on-chain reviews, live reference editing, wallet confirmations or a complete accessibility audit. The live matrix below still applies.

## Live harness — explicit approval required

`tests/integration/test_live_flow.py` is a separately invoked, write-producing test. It launches `scripts/live-studionet.mjs` using the application's pinned `genlayer-js` client. It creates a new contract and two fresh test accounts and publishes fictional information. It never uses the developer’s saved wallet. Do not run it merely to collect tests or check syntax.

After approving the target environment and network writes, use the locked environment:

```powershell
$env:REPLYCHECK_RUN_LIVE='1'
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
$env:PYTHONIOENCODING='utf-8'
gltest -p gltest_cli.config.plugin tests/integration/test_live_flow.py -v -s --network studionet
```

This harness targets only the built-in Studionet profile and verifies chain ID 61999 before sending. To reuse a previously verified ReplyCheck deployment, explicitly set `REPLYCHECK_CONTRACT` to that full address; source bytes are checked again before any workspace write. Each run still creates a separate fictional workspace and disposable accounts. No automatic resubmission occurs after an unknown outcome.

The harness asserts execution success using the application's receipt parser, verifies exact calldata and zero value, compares deployed source bytes, checks supported/contradicted/unsupported cases, approves a card and tests staleness/retirement, member revocation, ownership transfer and archive/restore. It checkpoints whitelisted public inputs, receipt records, stored chain timestamps, expected reason codes and returned payloads under `evidence/studionet/`, including incomplete runs. The full live matrix below remains required even if this harness passes.

The September 10 deployment exposed two transport issues before model testing: Python SDK 0.2.16 treats several hosted Studio methods as testnet methods, and the JavaScript SDK's Ethereum receipt formatter drops Studio's numeric zero value. The live harness uses the pinned app SDK without changing chain IDs; the app reads raw Studio receipts. Final-round verification binds the unique actual leader to the final accepted consensus history, distinguishing idle validator entries from the leader's execution result. A sanitized real deployment receipt and adversarial mutations are regression-tested.

The first deployment (`0x3B75D90Cf55E5b3D45ab748988F0C98cD6a70C41`, transaction `0x186cc40daeb1c4be2df9c2dec9fed28eb472dd383342b0b21485b2d648175228`, source `e6ea9e181cd30b3ae35e54984c5d7dd086b53e188b3b8881b46622796ccf4ce7`) is superseded: the model test mislabeled absent evidence as contradiction and related-topic text as an answered question. The test expectation was retained, not relabeled as a pass. Shared leader/validator/auditor rules now explicitly distinguish claim scope, absence of evidence and question coverage.

The second deployment (`0x31F43259D3c70b00a2891Caf5B4Ee2e01b6c8F81`, source `6c4fffa9aece2522833c1ce923fa3f9c8cd8d1b4a7c8ffbdaa59e63a900406fc`) corrected that classification and passed four model cases, but exposed missing neutral-output instructions in the greeting case. It is also superseded. The current deployment and incident records are listed in [LIVE-VALIDATION.md](LIVE-VALIDATION.md).

The full live matrix asserts question coverage, mixed claims, unanswered questions, evaluator-instruction resistance, explicit permission rejections and idempotent replay. Exact source/harness snapshots are retained with their hashes. `node scripts/verify-live-evidence.mjs evidence/studionet/<run-id>.json` replays a completed run through the current app verifier without sending transactions; it refuses incomplete runs. These are local evidence records, not an immutable GitHub commit or public Ubuntu result.

## Human wallet and browser matrix

Latest scoped result (September 13 UTC): [two-tab guards and unrelated-hash rejection](evidence/human-wallet/20260913-two-tab-hash-reconciled.json) passed on the unchanged recovery-fixed app. Two pre-staged Continue attempts were dispatched; the competing tab hit the wallet lock, then denied further valid drafts and recovery mutation while the primary held its request. After the single approval, a real unrelated receipt was rejected without replacing the original hash. Raw before/after exports matched exactly; the final successful call/return/source/full state prove only one new review (14 → 15). [One verified acknowledgment cleared both tabs](evidence/human-wallet/20260913-two-tab-browser-close.json). This does not claim every concurrency or legacy-journal replacement path. The missing-hash browser attempt kept recovery safe but showed generic connection feedback; a later direct read returned ResourceNotFoundRpcError, exposing both a null-response assumption in the read harness and a wording improvement to make. Original unsuccessful/inconclusive outputs are preserved in the [finding](evidence/human-wallet/20260913-missing-hash-wording-finding.json).

The September 10 automated Studionet run completed successfully: **26 transactions, six expected rejections and all six model fixtures**. Its exact evidence and preserved earlier failures are linked in [LIVE-VALIDATION.md](LIVE-VALIDATION.md). The configured app client independently re-read the deployed source, all six reviews and final workflow state successfully. These checks did not use a human wallet or a browser extension.

The [in-progress Chrome/MetaMask record](evidence/human-wallet/20260910-105817.json) verifies connection rejection and approval. It also preserves a real workspace-form failure: six app-owned primary buttons needed explicit `type="submit"`. Eight new source-level regressions caught the missing declarations before the fix. After rebuilding, Chrome verified mouse/Enter creation staging, the unchecked-consent gate, disabled empty-name submission, workspace/review lookup, historical reference browsing and the non-submitting return action. Member, reviewer and editor invitations/acceptances, ownership transfer, former-owner access revocation and archive/restore have since passed. The full wallet matrix is **not complete**.

Wallet A has rejected and retried workspace creation, created its owner workspace and published reference v1. The saved [creation](evidence/human-wallet/20260910-105817-create-workspace.json) and [reference publication](evidence/human-wallet/20260910-105817-reference-v1.json) records bind final successful execution to exact calldata and stored state.

All six human-wallet reply fixtures now match their predeclared classification expectations. The [closing batch check](evidence/human-wallet/20260910-105817-reply-batch.json) verifies the sidecar hashes, saved final execution/calldata, deployed source and fresh reads of all six complete stored reviews. Earlier review payloads and the reference bundle remained unchanged.

| Fixture and exact evidence                                                           | Overall verdict        | Question coverage | Reason codes                       |
| ------------------------------------------------------------------------------------ | ---------------------- | ----------------- | ---------------------------------- |
| [Correct](evidence/human-wallet/20260910-105817-correct-reply.json)                  | MATCHES_REFERENCES     | ANSWERED          | CLAIM_SUPPORTED                    |
| [Contradictory](evidence/human-wallet/20260910-105817-contradictory-reply.json)      | NEEDS_CHANGES          | ANSWERED          | CONTRADICTS_REFERENCE              |
| [Unsupported](evidence/human-wallet/20260910-105817-unsupported-reply.json)          | NEEDS_CHANGES          | UNANSWERED        | NOT_IN_REFERENCES                  |
| [Mixed claims](evidence/human-wallet/20260910-105817-mixed-reply.json)               | NEEDS_CHANGES          | ANSWERED          | CLAIM_SUPPORTED; NOT_IN_REFERENCES |
| [Greeting only](evidence/human-wallet/20260910-105817-unanswered-reply.json)         | NOT_ENOUGH_INFORMATION | UNANSWERED        | NON_FACTUAL_TEXT                   |
| [Evaluator manipulation](evidence/human-wallet/20260910-105817-injection-reply.json) | NEEDS_CHANGES          | ANSWERED          | CONTRADICTS_REFERENCE              |

The mixed reply is recorded as `PASS_WITH_NOTE`: “Refund processing time may vary” is qualified wording allowed by the current uncertainty rule, but “The references do not specify refund processing time” would express the evidence limit more clearly. This editorial review remains open before submission; the original output is preserved. The injection fixture demonstrates resistance to that exact input, not every possible attack.

The public correct-review export matched its complete stored review/reference bundle. Editing the draft, changing only the question and applying a suggestion each cleared the prior assessment; applying a suggestion explicitly required a fresh check without sending a transaction.

The user has identified Wallet B as `0x7Cef5DBbD598ba74EF9C665c9853E573448d97D0`. Its [read-only preflight](evidence/human-wallet/20260910-105817-wallet-b-preflight.json) verified a visitor role and no existing invitation. The owner invitation form passed browser staging, and the [Wallet A invitation](evidence/human-wallet/20260910-105817-invite-wallet-b.json) finalized successfully with exact calldata and zero value. Wallet B received a pending Member invitation but remained a visitor outside the membership list. A [fresh resume check](evidence/human-wallet/20260910-105817-invitation-resume-20260911.json) confirmed that state after restarting the stopped local preview, and Chrome recovered the same already-finalized transaction without a new wallet request. This is not yet a test of interruption during an unknown/unfinalized transaction.

On September 11, Chrome verified the switch to B, visitor access, hidden owner/reference-edit controls and a disabled Review draft button even with valid public inputs. The user then approved the [separate Wallet B acceptance](evidence/human-wallet/20260910-105817-accept-wallet-b.json). Final successful execution, exact sender/calldata, zero value and matching deployed source were verified before reading state: B is a member, its invitation is consumed and A remains owner, with version 1, six reviews and zero cards unchanged. The saved public receipt/export and workspace delta also passed offline replay. As a member, B could view a current supported review but had no Approve answer card control; reference editing and owner controls remained absent.

The same valid draft then enabled Review draft. The user-approved [Wallet B member review](evidence/human-wallet/20260910-105817-member-reply-wallet-b.json) finalized successfully with the exact public calldata and zero value. Its result is `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`, authored by B and recorded at the stored chain timestamp `2026-09-11T07:18:56.168799+00:00`. Only the workspace review count changed, from six to seven. The decoded final leader return payload matched the complete stored review, and the browser's assessment export matched the complete review/reference bundle. This proves one member-authorized call in addition to the earlier visitor UI denial; it does not replace the six Wallet A classification fixtures. Subsequent role, card-lifecycle and revocation milestones are recorded below; interrupted/concurrent sessions and the full human/browser matrix remain incomplete. No public Ubuntu result or immutable GitHub commit is claimed for these local evidence files.

The reviewer invitation also finalized; a question about an absent new wallet popup was clarified in the human-wallet ledger without resending the transaction. B's [separate reviewer acceptance](evidence/human-wallet/20260910-105817-accept-reviewer-wallet-b.json) finalized successfully and consumed the invitation, with the entire seven-review workspace unchanged. Reviewer UI checks showed answer-card approval available while reference editing and owner actions remained absent.

B then [published the first answer card](evidence/human-wallet/20260910-105817-publish-card-reviewer-wallet-b.json), `Refund request window`, with exact finalized calldata, zero value and the stored approval time `2026-09-11T08:14:32.125629+00:00`. The card was approved, linked to B's verified v1 review and contained its exact question/reply; only card_count increased from zero to one. The decoded final return matched the stored fields (excluding computed status), and the source review/reference bundle remained unchanged. Chrome displayed the approved card with the correct approver and version. Use in draft copied the exact inputs while clearing the displayed assessment and requiring a fresh check before another approval, without sending a transaction. The card was deliberately kept active for the reference-version invalidation test below; updated-card approval and old-card retirement are recorded below.

A's [editor invitation for B](evidence/human-wallet/20260910-105817-invite-editor-wallet-b.json) finalized successfully while B remained a reviewer until separate acceptance. B's [editor acceptance](evidence/human-wallet/20260910-105817-accept-editor-wallet-b.json) then finalized successfully, consumed the invitation and changed B to editor. Its new hash and exported editor effect distinguish it from earlier acceptances with identical calldata. The complete workspace, approved card and reference v1 bundle remained unchanged; saved receipts/exports and state comparisons passed offline replay.

Editor UI checks confirmed reference editing is available while owner actions, answer-card approval and retirement controls are absent. The original review and answer card remain readable.

B's [reference version 2 publication](evidence/human-wallet/20260910-105817-reference-v2-editor-wallet-b.json) finalized successfully with exact calldata and zero value. The fictional refund window changed from 7 to 14 days, with stored publication time `2026-09-11T09:02:52.824673+00:00`. The final leader return matches the entire v2 bundle. Only workspace.version changed; the old card became `NEEDS_RECHECK` with its original fields untouched. Reference v1 and all seven complete earlier reviews, including their original timestamp strings, remained unchanged. Saved evidence passed offline replay.

Chrome verified the stale editor cannot republish after recovery is acknowledged, version 1 remains readable as Historical and Return to current restores v2. The old card shows Needs recheck without Copy or Use in draft. Approved/Needs recheck/Retired/All statuses filters return 0/1/0/1 cards. Recheck reply loads the exact old 7-day answer under v2 and clears the prior assessment without submitting.

The human-approved [fresh v2 check of the old answer](evidence/human-wallet/20260910-105817-stale-answer-v2-wallet-b.json) finalized successfully and returned the expected `NEEDS_CHANGES / ANSWERED / CONTRADICTS_REFERENCE`. It cites the exact 14-day policy and suggests the matching 14-day correction. The new review's stored timestamp is `2026-09-11T09:13:54.547292+00:00`; only review_count increased from seven to eight. The final return and browser assessment export match the complete stored review/reference bundle. Both reference versions, all seven earlier reviews and the stale card remain unchanged.

Apply suggestions replaced only the draft with the exact 14-day correction, cleared the previous assessment and required a fresh check without submitting.

The separately approved [corrected-v2 review](evidence/human-wallet/20260910-105817-corrected-answer-v2-wallet-b.json) finalized successfully with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`, the exact 14-day citation and no suggested rewrite. Its stored timestamp is `2026-09-11T09:25:46.648796+00:00`. Exact public nonce/calldata, derived identifiers, final return and both browser exports were verified. Only review_count increased from eight to nine; all earlier reviews, both reference versions and the old stale card remain unchanged. After acknowledging recovery, B still could not approve the matching current review as an editor. The subsequent account switch verified A's owner permissions and restored the approval control for this exact B-authored review.

A then [published the distinct v2 answer card](evidence/human-wallet/20260910-105817-publish-card-v2-owner-wallet-a.json), `Refund request window (v2)`, from that corrected review. The exact transaction finalized successfully with zero value and stored approval time `2026-09-11T09:37:23.444488+00:00`. Public calldata, UUID, call digest and recovery export were bound to the exact receipt; the final consensus leader return matches the complete stored fields excluding computed status. The new 14-day card is `APPROVED`, not retired and approved by A rather than source-review author B. Only card_count increased from one to two. Both reference bundles, all nine complete reviews, roles and the original unretired `NEEDS_RECHECK` card remain unchanged. The saved proof passed offline receipt/call/export/return/state/pin replay and ledger binding before recovery acknowledgment. Chrome displayed both cards with the correct distinct content, versions, approvers and permitted actions. Filtering to Needs recheck isolated only the old v1 card for its separate retirement.

A's [retirement of the original v1 card](evidence/human-wallet/20260910-105817-retire-v1-card-owner-wallet-a.json) finalized successfully with exact calldata, zero value and the expected null final leader return. Only the original card's retired flag and computed status changed to `true / RETIRED`; its original fields and chain approval time remain intact. The new v2 card stays `APPROVED`. Both reference bundles, all nine complete reviews, roles and the whole workspace are unchanged. The contract stores no retirement timestamp; raw receipt creation time `2026-09-11T10:13:28.466622+00:00` is retained without calling it a stored retirement or finalization time. Saved evidence and ledger binding passed offline replay before acknowledging recovery.

Post-retirement Chrome checks showed Needs recheck/Retired/Approved/All statuses counts of 0/1/1/2. The retired card has no Copy, Use in draft, Recheck reply or Retire control, while View review remains enabled. That link opens the exact original v1 review with its historical-version warning, B author, original timestamp, digests and 7-day citation; it cannot be approved as current. The historical review still offers copying and a fresh recheck, which retirement does not prohibit. The new approved v2 card retains its enabled copy/reuse controls. The filter was restored to All statuses.

A's [removal of editor B](evidence/human-wallet/20260910-105817-remove-editor-wallet-b.json) finalized successfully with the exact `remove_member` calldata, zero value and null final consensus leader return. B became `visitor`, its invitation is empty, and the only workspace change is member_addresses from `[B]` to `[]`; A stays owner. Both complete cards, both reference versions and all nine full reviews remain unchanged. Raw receipt creation time `2026-09-11T10:23:53.259930+00:00` is preserved separately from observation time, not claimed as a stored revocation or finalization time. The proof/export and all receipt, return, state and pin checks passed offline replay before recovery acknowledgment. Chrome removed B from the owner team list and retained A's enabled owner controls with no pending transaction. No actual pending invitation was evidenced before removal, so cancellation of an outstanding invitation is not claimed by this step.

The subsequent human switch to B passed revoked-account browser checks recorded in the [human-wallet ledger](evidence/human-wallet/20260910-105817.json). Chrome showed visitor access, no invitation acceptance or owner controls, and no pending operation. With the exact valid corrected 14-day draft and a current matching review visible, Review draft was disabled, Approve answer card absent and Refresh enabled. Reference editing and card retirement controls were absent. Both cards, all nine history entries and the exact current/historical B-authored reviews remained readable. Historical v1 references loaded with their original text/digest, and Return to current restored v2. These are UI permission checks, not a new human-signed forbidden contract call.

The switch back to owner A passed. A [read-only ownership preflight](evidence/human-wallet/20260910-105817-ownership-preflight.json) reverified the previous successful removal receipt before source/state reads, confirmed no existing ownership proposal and preserved the complete active workspace, both card records, both reference bundles and all nine reviews. A remained owner and B a visitor without an invitation. The preflight and future-call digest passed offline replay. The ownership form rejected empty, malformed and current-owner targets, and allowed the known B address. The unsigned confirmation showed full B as target, A as the exact signing wallet and unchecked consent before the user approved the proposal below.

A's [ownership proposal to B](evidence/human-wallet/20260910-105817-propose-owner-wallet-b.json) finalized successfully with exact `propose_owner` calldata, zero value and a null final consensus leader return. Only the proposed-owner record changed from empty to B. The complete workspace is unchanged: A stays owner, B remains visitor without a role invitation, no members, reference v2, nine reviews and two cards. All full review, reference and card records are preserved. Raw receipt creation time `2026-09-11T10:45:49.345334+00:00` is retained, not presented as a stored proposal or finalization timestamp. Saved receipt/export, output, state, pins and preflight binding passed offline replay before acknowledgment. Chrome then showed full B as Proposed owner while A retained enabled Cancel transfer and Archive workspace controls; A had no Accept ownership control. No cancellation was performed. Switching to B for separate ownership acceptance now awaits the user; ownership has not transferred and the broader human-wallet matrix remains incomplete.

On September 12, the existing local production preview was restarted without a rebuild. A transient dependency-file lock and Studionet DNS failure were resolved before proceeding. The [read-only ownership resume check](evidence/human-wallet/20260910-105817-ownership-resume-20260912.json) reverified the original proposal's exact successful receipt and null return before checking deployed source and current state. All 12 comparisons passed: A remains owner, B visitor and proposed owner, both invitations are empty, and the complete workspace, both reference versions, both cards and all nine reviews remain unchanged. The saved proof passed offline replay. Chrome was restored to Team with A connected and B shown as Proposed owner. No new transaction or ownership acceptance was sent; the user must switch the site-connected wallet to B next. This resume check does not claim interruption recovery for an unknown or unfinalized transaction.

The human switch to B then passed: Chrome showed B as visitor and proposed owner with Accept ownership enabled, while cancellation and archive controls were absent. A [focused acceptance preflight](evidence/human-wallet/20260910-105817-accept-ownership-preflight-wallet-b.json) reverified the existing proposal before checking matching source, unchanged complete workspace, roles, nomination and empty invitations. The unsigned Accept workspace ownership confirmation displays B's exact full signing address and zero contract payment, with consent unchecked and Continue to wallet disabled. This separate acceptance now awaits the human's review and MetaMask approval; ownership has not yet transferred.

The [human-approved ownership acceptance](evidence/human-wallet/20260910-105817-accept-ownership-wallet-b.json) subsequently finalized successfully. Exact B sender, zero value, `accept_ownership` calldata, predeclared digest and public recovery export were verified before source/state reads. The actual final consensus leader returned null. Only workspace.owner changed A to B: B is owner, A visitor, both invitations are empty and the proposal is consumed. Workspace active status, empty membership list, version 2, both full cards, both complete reference bundles and all nine complete reviews are preserved. The raw receipt creation time is `2026-09-12T12:37:37.361136+00:00`; the method stores no separate acceptance timestamp. All 13 comparisons and saved evidence replay passed before recovery acknowledgment.

New-owner B browser checks then passed: Team shows full B as owner, invitation/transfer forms and enabled Archive workspace, with no pending nominee or acceptance/cancellation controls. Edit references and retirement of the current approved card are enabled; the old retired card remains read-only at card level. No edit, retirement or archive was performed. The page was returned to Team with no recovery or unsigned dialog. Switching to former owner A for browser-side access-revocation checks now awaits the user; the full human/browser matrix and archive/restore checks remain open.

The subsequent former-owner A switch and browser access checks passed, as recorded in the [human-wallet ledger](evidence/human-wallet/20260910-105817.json). A is a visitor with B still owner. Invitation/revocation, ownership, archive, reference-edit, approval and retirement controls are absent. Reusing the approved public card loaded the exact valid 14-day draft, but Review draft remained disabled with the invitation-required message. The current matching review cannot be approved by A. Both cards, all nine history entries, the current and historical matching reviews and both reference versions remain readable; historical v1 was opened and Return to current restored v2. Checks are bound to the unchanged saved acceptance proof. No new transaction was sent. Team is left with A connected and no recovery or unsigned dialog; switching back to B for the separate archive/restore tests now awaits the user.

B's switch back to the owner account passed. The [read-only archive preflight](evidence/human-wallet/20260910-105817-archive-preflight-wallet-b.json) reverified the successful ownership-acceptance receipt before matching-source and fresh complete-state checks. All 13 checks passed with the workspace still active and all history unchanged. The future `set_archived [workspace, true]` input, zero value, null return and exact digest are predeclared. Only workspace.archived should change in stored data; the current card's computed read status should become `NEEDS_RECHECK`, while the retired card remains `RETIRED`. The unsigned Archive workspace confirmation is staged with the exact full B signer, unchecked consent and disabled Continue to wallet. Human approval is pending; neither archive nor restore has been completed.

The [human-approved archive](evidence/human-wallet/20260910-105817-archive-workspace-wallet-b.json) finalized successfully with exact B sender, zero value, calldata/digest, public export and null final consensus leader return. Strict execution checks preceded source/state reads. Only workspace.archived changed to true in stored data. B remains owner and A visitor, with empty invitations/proposal; both full reference bundles, all nine reviews and all original card fields/timestamps are unchanged. The current v2 card's computed status is `NEEDS_RECHECK`; the retired v1 card remains identical. Raw receipt creation time `2026-09-12T12:56:39.998818+00:00` is preserved, not described as a stored archive or finalization time. All 15 comparisons and offline evidence replay passed before acknowledgment.

Archived-state browser checks passed for owner B: valid-draft review submission is disabled with the archived message; invitation/revocation/transfer, reference editing, answer-card approval and retirement controls are absent. Current references, both cards, nine history entries and recorded assessments stay readable. Card-level Copy and Use in draft are absent; local recheck preparation and public assessment copying do not grant permission to submit. Restore workspace remains enabled. The [restoration preflight](evidence/human-wallet/20260910-105817-restore-preflight-wallet-b.json) then reverified the archive receipt and unchanged complete archived state. The separate unsigned restore is staged for B with unchecked consent and zero payment, awaiting human approval. It should reactivate the existing current card without re-reviewing it; no restoration has occurred yet.

The [separate human-approved restoration](evidence/human-wallet/20260910-105817-restore-workspace-wallet-b.json) then finalized successfully. Exact B sender, zero value, `set_archived [workspace, false]` calldata/digest, public export and null final consensus leader return were verified before source/state reads. Only workspace.archived changed back to false in stored data. The current unretired v2 card returned to `APPROVED` without another review or publication; old v1 stays `RETIRED`. B remains owner and A visitor, with all references, nine reviews, original card fields and timestamps intact. The complete active public state exactly matches the hash-bound pre-archive checkpoint. Raw receipt creation time `2026-09-12T13:07:11.278856+00:00` is preserved without inventing a stored restoration or finalization time. All 16 comparisons and offline replay passed before acknowledgment.

Restored-state browser checks passed: owner invitation/transfer forms, Archive, valid-draft Review draft, Edit references, current-card Copy/Use in draft/Retire and current-review Approve answer card controls are available again. The retired card remains retired and all nine history entries remain visible. No new review or approval was submitted to test reactivation. The page is left on the current matching review with B connected, no recovery and no unsigned action. The human archive/restore milestone is complete; interrupted/unknown transaction recovery, concurrent sessions, broader adversarial/browser checks and public release gates remain open.

The [unsigned account-switch check](evidence/human-wallet/20260910-105817-account-switch-unsigned-b-to-a.json) passed in two Chrome tabs. The new tab initially held `Check this reply` for the exact B signer and public 14-day fixture, with unchecked consent and disabled Continue to wallet. After the human switched to A without signing, the old confirmation closed and both tabs showed A as visitor. Both preserved the valid draft but disabled Review draft with the invitation-required message; no confirmation or recovery panel remained. The observer kept all nine history entries and its current review, without Approve answer card. The existing restoration receipt was reverified as successfully executed with exact calldata before source/state reads. The deployed source and all ten complete public-state outputs remained identical to the hash-bound restoration checkpoint. All twelve chain comparisons and offline receipt/state/browser assertions passed. No new transaction was sent. The [human-wallet ledger](evidence/human-wallet/20260910-105817.json) preserves the exact preconditions and observations; switching back to owner B is the next human setup step. This only closes identity invalidation with consent initially unchecked, not consent-already-checked, network changes, an actual cross-tab send-lock race or interrupted recovery.

Returning to owner B passed in both tabs without restoring the discarded confirmation or creating recovery metadata. A fresh unsigned `Check this reply` was then staged for the separate network-change test with the exact B signer, unchecked consent and disabled Continue to wallet. The [human network-switch check](evidence/human-wallet/20260910-105817-network-switch-unsigned-wallet-b.json) passed: the confirmation closed without agent cancellation, both tabs show Switch to Studionet, and the valid retained draft cannot be submitted. Neither tab has a dialog or recovery panel; the observer retains all nine history entries and its current assessment without Approve answer card. The existing restoration receipt was reverified as successfully executed with exact calldata before source/state reads; deployed source and all ten complete public-state outputs match the hash-bound account-switch checkpoint. All twelve chain comparisons and offline receipt/state/browser assertions passed. No new transaction was sent. Ethereum was the requested target, but the app's generic off-network indicator proves only not-Studionet; exact alternative wallet network name and chain ID remain unobserved. Return to Studionet now awaits the human before the separate pending/two-tab/reload tests. Consent-already-checked and network changes during pending transactions remain outside this completed case.

The subsequent return to Studionet passed in both tabs with B recognized, the discarded confirmation still closed, the valid draft preserved and owner actions re-enabled. A fresh [pending-wallet/two-tab preflight](evidence/human-wallet/20260910-105817-pending-wallet-two-tab-preflight.json) reverified successful prior execution before source/state reads; all thirteen checks and offline replay passed with exactly nine reviews and two cards unchanged. The existing fictional 14-day inputs, request/reference digests and expected later one-review delta are predeclared. The newly generated nonce is not exposed in the unsigned dialog and is deliberately left unknown until it can be bound to a public recovery export and exact receipt calldata.

The [held-request observer-reload check](evidence/human-wallet/20260910-105817-held-wallet-observer-reload.json) subsequently passed all seven recorded browser assertions. The human opened the wallet request in tab `329573059`, reversing the planned tab roles. Its disabled Waiting for wallet and Cancel controls identified the real sender before any reload. The other tab showed WALLET OUTCOME UNKNOWN and denied the same valid draft. Only that non-sending tab's unsigned dialog was cancelled; its exact fictional draft was recorded, cleared for reload and re-entered afterward. The warning, hash-recovery input and submission denial survived reload while the original sender stayed waiting. This proves observer-side gating and reload persistence, not a simultaneous-send race or recovery after signing. The preserved proof passed offline replay after the file relocation was resolved.

The held request was then rejected by the human while the relocation was being diagnosed. The [post-rejection checkpoint](evidence/human-wallet/20260910-105817-post-rejection-relocation.json) records the explicit wallet-declined message, no dialog/recovery/waiting state, and the valid draft enabled again. Strict successful execution and exact calldata for the already-completed restoration were reverified before deployed-source and fresh full-state reads. All ten public state outputs remained identical to the pre-held-request checkpoint: B owner, A visitor, active v2 workspace, nine complete reviews, both original reference bundles, two cards and no invitations/proposed owner. Raw stored timestamps remain unchanged. No new review hash or completed review is claimed; the prior receipt is not execution evidence for the rejected request.

The preview was restarted from the new local folder and checked again in Chrome. The same owner, reference digest/text, nine history entries and approved-v2/retired-v1 cards remained visible, with no pending wallet request. Only the known fictional draft was cleared and restored for that relocation reload; this is not a draft-preservation test. The held-request artifact's historical approval instruction is superseded: **there is no old request left to approve**. A fresh action with separate human approval was used for the after-signing case below.

The subsequent [after-signing recovery proof](evidence/human-wallet/20260910-105817-after-signing-recovery-wallet-b.json) passed seventeen exact checks. Fresh human-approved transaction `0xd968466ecff091c494e36e75646c6c70ea8e6ed85c61a592b8f21d10e68993fb` was ACCEPTED immediately before reloading the same sending tab at 15:04:27 UTC. A public receipt observed after reload at 15:04:32 UTC was still ACCEPTED/pending; the saved hash then recovered and the app displayed final execution success. Before/after transaction exports are byte-for-byte identical. Strict receipt/calldata and deployed-source checks preceded full stored-state reads; the final leader return equals the complete stored review, with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`, the literal 14-day quote and raw chain `recorded_at` of `2026-09-12T15:03:29.200245+00:00`. Exactly one new review increased the count from nine to ten, with all prior reviews/timestamps, both reference bundles/cards, roles, invitations and proposed owner unchanged.

The [browser closing proof](evidence/human-wallet/20260910-105817-after-signing-browser-close.json) records an assessment export equal to the full stored review and reference bundle. Only after the exact proof was saved and replayed did the agent acknowledge the terminal recovery notice with Done. All ten history entries and the current assessment remain visible, owner approval is re-enabled, and no app confirmation, waiting control, unknown-outcome warning or recovery panel remains. No additional wallet request or resubmission was made. The attempted pre-reload draft clearing was not confirmed by the immediate DOM snapshot; automatic draft persistence, a simultaneous-send race, pending identity changes and hydrated valid-draft denial during the post-reload pending interval are not claimed. Observation times are distinct from stored chain timestamps. Broader recovery, adversarial, accessibility and public-release gates remain open.

Offline reproduction of this exact saved recovery evidence (no RPCs or wallet):

```sh
node evidence/human-wallet/replays/20260912-after-signing-recovery.mjs --historical-ui
```

Read-only reproduction for the currently configured deployment:

```sh
node scripts/verify-live-evidence.mjs evidence/studionet/749f58e6-6c2c-4401-9848-e2a03df7326e.json
node scripts/smoke-live-client.mjs evidence/studionet/749f58e6-6c2c-4401-9848-e2a03df7326e.json
```

The first command replays saved payloads. The second additionally makes fresh public RPC reads through the application's actual client. Neither sends transactions or opens a wallet. The human/browser matrix remains:

1. Use a current Chrome/Chromium browser with Web Locks and an EIP-1193 wallet. Record exact browser/wallet versions.
2. Reject connect, reject network switch, and reject a contract action. Verify no completed result appears.
3. Wallet A creates a workspace and publishes references. Record exact source bytes and transaction hashes.
4. A invites B; B must explicitly accept. Test each role’s allowed/forbidden actions and revocation.
5. Submit correct, contradictory, unsupported, mixed-claim, unanswered and injection-bearing public fictional drafts. Preserve full findings and literal references.
6. Edit a reviewed draft and apply a rewrite. Confirm the earlier result is no longer treated as approval of the edited draft.
7. Approve a matching current card, publish a new reference version, recheck and retire a card. Old evidence must remain unchanged.
8. Switch account/network while a confirmation is open and while a transaction is pending. No stale authorization or duplicated request should occur.
9. Exercise two-tab submission, slow consensus, reload after signing, rejected execution, a missing response/hash, a wrong recovered hash and conflicting receipt fields. Do not resend unknown operations.
10. Exercise rapid concurrent member acceptance, reference edits, ownership transfer and archive/restore. Verify all terminal outcomes can be reconciled.
11. Test keyboard-only interaction, mobile layouts, focus recovery, screen-reader announcements, copy/export, version browsing and direct review links.
12. Repeat the clean suite on Ubuntu and record the immutable tested commit. Test the same deployed build with reviewers signed out.

## September 13 UTC checked-consent account switch

The [checked-consent account-switch proof](evidence/human-wallet/20260913-checked-consent-account-switch.json) records eight scoped checks across twelve exact Chrome observations. The assistant checked only the local unsigned answer-card confirmation for owner B; Continue to wallet was never activated. The first human report still showed B with checked consent and was not counted as a pass. After the subsequent site-account switch, the first fresh observation showed A as visitor, no confirmation and no answer-card approval control, without agent cancellation, reload or navigation during that transition.

A's valid public 14-day draft remained blocked with the invitation-required message. Team management, reference editing and card retirement controls were absent. The same ten history entries, existing matching assessment, current references and two cards remained readable. Temporary known test inputs were cleared and History restored. All 24 current local source pins and three baseline artifacts match their recorded hashes. This UI-only case did not freshly re-read the deployed source/full chain state, execute a forbidden contract call, or produce a transaction hash/contract reason code. Browser observation times are separate from the existing displayed chain review time. Return-to-B consent reset, checked-consent network changes, pending operations, native multi-tab races and public release checks remain open.

Offline reproduction checks the saved observations and file hashes, not a new browser or wallet run:

```sh
node evidence/human-wallet/replays/20260913-checked-consent-account-switch.mjs
```

The existing 237-test local suite is unchanged; it was not rerun for this documentation/evidence-only follow-up. This new replay has not yet been added to the pinned public-workflow template, and no public CI pass is claimed.

The subsequent [return-to-owner proof](evidence/human-wallet/20260913-checked-consent-owner-return.json) records B recognized as owner with approval restored and the old confirmation still absent. Explicitly opening a fresh confirmation resets consent to unchecked and disables wallet continuation. The local checkbox was then checked only to stage a separate human network-change test. That next test is pending, with no wallet request or signature. Three observations and their three scoped assertions replay with `node evidence/human-wallet/replays/20260913-checked-consent-owner-return.mjs`; the prior evidence is hash-bound and unchanged. This closes the return-to-B/reset follow-up, not a network or pending-transaction case.

The later [checked-consent network-switch proof](evidence/human-wallet/20260913-checked-consent-network-switch.json) closes the scoped off-network portion with four checks/five observations. The first fresh post-human-switch snapshot shows Switch to Studionet and no confirmation or answer-card approval, without agent cancellation/reload. The known valid draft cannot be submitted and displays the Studionet-required reason. The same ten history entries and matching assessment remain readable; temporary inputs were cleared. Ethereum was requested, but only the generic off-Studionet condition is independently observed. Exact alternative chain ID, fresh deployed-source/full-state verification, pending-transaction behavior and the return to Studionet are not claimed. Replay with `node evidence/human-wallet/replays/20260913-checked-consent-network-switch.mjs`; no new wallet request or transaction was made, and the preceding staged-only proof remains unchanged.

The subsequent [Studionet-return proof](evidence/human-wallet/20260913-checked-consent-studionet-return.json) completes this single-tab unsigned round trip with four checks/seven observations. B's owner approval and valid-draft eligibility return without reviving the discarded confirmation. A freshly opened confirmation starts unchecked with Continue to wallet disabled; the agent cancels only that new local dialog, preserves its transient closing shell, and verifies the settled no-dialog state. Known temporary inputs are cleared and all ten visible reviews remain. Replay with `node evidence/human-wallet/replays/20260913-checked-consent-studionet-return.mjs`. Across the four September 13 proofs, 19 scoped checks and 27 observations are preserved; no wallet request or transaction was initiated. Source/tests remain unchanged, and the 237-test suite is not rerun for these evidence-only follow-ups. These four new replays still need inclusion in the eventual public-workflow checkpoint; public CI, pending-transaction changes, actual concurrent sends and broader release/accessibility gates remain open.

## Pending identity/network test preflight

The [September 13 pending-identity preflight](evidence/human-wallet/20260913-pending-identity-preflight.json) passes twelve read-only checks before a new human request. The application's actual receipt verifier confirms the previous transaction's exact successful execution/calldata/zero value and final leader return before the deployed-source/protocol and complete stored-state reads. All ten current public outputs match the preceding ten-review checkpoint. The future single B review uses the public 14-day fixture, reference v2 and a fresh app-generated UUID; the UUID, review ID and call digest remain unknown until bound to the public transaction export/receipt. Expected assessment codes and the sole ten-to-eleven review-count delta are predeclared. No previous hash is reused or resent.

An initial DNS lookup failed before state reads; a later command succeeded but its warning-prefixed output was not captured as evidence. Both interruptions are preserved, and only the successful captured retry supplies this proof. The prior-receipt timestamp is explicitly identified as request-start time; stored timestamps remain exact and subsequent state observation timestamps follow completed reads. Four browser observations record B ready with no recovery, a valid draft and one unsigned Check this reply dialog with unchecked consent and disabled continuation. The human must approve this one request and stay on B until its hash/pending receipt is captured. Account/network changes during pending execution are not claimed yet. Replay offline with `node evidence/human-wallet/replays/20260913-pending-identity-preflight.mjs`. The live generator is read-only: `node evidence/human-wallet/preflights/20260913-pending-identity-network.mjs`.

### First pending-identity attempt: transaction succeeds, timing case not exercised

The human approved transaction `0xeb94cb35bc3bb2d21d8c65059f43a79c5775091b63df40fa3da1ea33e8b7fb99`. The first Chrome snapshot displayed accepted with the valid draft disabled, but the first independent receipt captured at `2026-09-13T06:30:29.068Z` was already finalized successfully, before any account/network switch. This is preserved as `NOT_EXERCISED_FINALIZED_BEFORE_SWITCH`, not a pending-identity pass. The [reconciliation](evidence/human-wallet/20260913-first-identity-attempt-reconciled.json) verifies the exact zero-value call, UUID `4cd1a7c5-0372-492a-8dd1-90f5cc302d98`, derived review `58fd2f95b95e7d6c2a9cd5a8d783580a1e4afb8cca747a53aa68ca085a43fb3c`, final leader return and stored review. The original chain `recorded_at` is `2026-09-13T06:28:43.492034+00:00`, with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`. Exactly one review increases ten to eleven; every earlier full review/timestamp, reference/card, role/invitation and workspace setting is preserved. Replay with `node evidence/human-wallet/replays/20260913-first-identity-attempt.mjs`.

Only after that proof passed was its terminal recovery notice acknowledged. The [second setup](evidence/human-wallet/20260913-pending-identity-second-setup.json) prepares a fresh app-generated UUID and a possible eleven-to-twelve delta; it never resends the completed hash. The human sequence is now approve once as B, immediately switch the site-connected account to A on Studionet, then report done. A prebuilt read-only receipt capture avoids writing scripts during the pending window. No pending-transition pass is claimed without an independently observed pending receipt alongside the changed account. Replay setup with `node evidence/human-wallet/replays/20260913-second-identity-setup.mjs`. No second wallet request was initiated by the agent.

### Second pending-identity attempt: account changes, pending timing remains inconclusive

The distinct second human-approved hash is `0x508dfeec460005432da63ba4742e797ff6f901ff9f68b8af372e511f9753d72e`. The first DOM request at `2026-09-13T06:48:06.189Z` returned B/committing. The next sampled state shows A as visitor and completed recovery still attributed to B. The first independent receipt was obtained at `2026-09-13T06:52:58.708Z`, already FINALIZED with exact successful execution. The account switch is visible, but its order relative to finalization is not established: **`INCONCLUSIVE_PENDING_SWITCH_NOT_CAPTURED`**, not a pending-account pass and not a demonstrated product failure. No pending-network change was attempted. The [capture record](evidence/human-wallet/20260913-pending-identity-second-receipt.json) preserves the initial ambiguous Export JSON selector and missing-file interruption before the successful region-scoped export. Browser timestamps mark snapshot request starts, not wallet events; receipt request/observation times and chain timestamps are kept separate.

The [second reconciliation](evidence/human-wallet/20260913-second-identity-attempt-reconciled.json) passes seven verification groups. Strict receipt/calldata verification precedes source/protocol and full stored-state reads. Fresh UUID `a61a9ef4-3ab7-4088-86c9-dfc2ec0472c8` binds review `ad8841a79d359586ef54e598cd36b38f114d7591a69f1cf015a1e6f49bc71c46` to B, reference v2, the exact public question/draft and zero attached value. The full leader return equals the stored review, with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`, the literal 14-day quote and raw chain `recorded_at` of `2026-09-13T06:47:46.348794+00:00`. Exactly one new review increases eleven to twelve. All earlier full reviews/timestamps, both reference versions/cards, roles/invitations and settings remain unchanged. A's valid draft stays disabled while B remains the original recovery signer.

```sh
node evidence/human-wallet/replays/20260913-second-identity-attempt.mjs
```

This command replays saved evidence offline, without RPCs or wallet actions. The separate live read-only generator is `evidence/human-wallet/preflights/20260913-reconcile-second-identity-attempt.mjs`; its exact twelve-review checkpoint must not be treated as a general live monitor after state changes. Neither completed attempt is resent. No third approval is requested: future timing work must first prepare an observer that can capture the changed visible account and the same public pending receipt together. Application source and the previous 237-test checkpoint are unchanged; the full suite is not rerun for these evidence/documentation additions. New September 13 replays still need inclusion in the later public-CI checkpoint, and public release, concurrent-send, pending-network and broader accessibility gates remain open.

After the saved transaction proof passed offline replay, the [browser close record](evidence/human-wallet/20260913-second-identity-browser-close.json) verifies four scoped checks across four observations. Only the completed recovery and its notice are acknowledged. A remains visitor, all twelve stored UTC timestamps are visible in History, and the latest review ID/assessment is opened successfully. The transient loading state is preserved separately from the settled result. The same replay command validates this closing record. No new confirmation or wallet request is staged, and no pending-transition pass is added.

### Third attempt preparation: arm observation before asking for approval

The user requested a retry after the second inconclusive timing capture. The [third preparation](evidence/human-wallet/20260913-pending-identity-third-preparation.json) freshly reverifies the completed second transaction and deployed source, then compares all ten complete public outputs with the saved twelve-review baseline. Three parser self-checks distinguish the historical B/committing state, A/finalized state and a clear page; they are not live pending-switch evidence or extra app-suite tests. Chrome capture/export helpers are prepared in the existing session. They use only visible DOM and the region-scoped recovery export; all old hashes are prohibited.

At this recorded preparation checkpoint A is still the visible visitor, no confirmation is staged, and continuous observation has not started. First switch the site's connected account to B on Studionet. After staging one fresh public-fixture confirmation, start active capture **before** asking the human to approve once and switch immediately to A. The pass criterion requires an exact public pending receipt bracketed by visible A/pending/B-signer observations; the next expected state delta is twelve to thirteen. Neither old attempt is resent or relabeled. Replay the preparation offline with `node evidence/human-wallet/replays/20260913-third-identity-preparation.mjs`.

### Third attempt interruption: human-reported cancellation

After B was observed as owner, one fresh unsigned confirmation was staged and active sampling began before the approval instruction. The [cancellation observation](evidence/human-wallet/20260913-third-identity-cancelled-observation.json) preserves 32 DOM samples across two bounded cycles, including their inter-cycle gap. No new hash or independent receipt window was captured. The page subsequently showed A as visitor with a hashless `WALLET OUTCOME UNKNOWN` warning, and the human reported that the transaction was cancelled. This is not a pending-account pass or a proven application failure. The cancellation callback/error code and cause of the retained uncertain record were not independently observed.

Fresh read-only verification confirms all ten complete public-state outputs still match the twelve-review baseline. The returned Studio history has fourteen B-to-ReplyCheck transactions; the newest is the already verified second-attempt hash. The SDK-declared optional history filter was rejected by the server, so the successful diagnostic used the address-only request and local filtering. That interruption is preserved. Public-history absence can lag and does not prove non-broadcast: the app's recovery record remains untouched until the human checks MetaMask Activity. If a transaction exists, recover its exact hash; otherwise the human can acknowledge `I checked: nothing was sent`. No replacement review or signing request is staged. Replay the saved observation with `node evidence/human-wallet/replays/20260913-third-identity-cancellation.mjs`.

The subsequent [human no-send acknowledgment](evidence/human-wallet/20260913-third-identity-no-send-acknowledgment.json) records the user's done reply and the cleared uncertain-recovery warning. A remains visitor with twelve history timestamps visible; no new confirmation or hash is staged. The agent did not click the acknowledgment or independently inspect MetaMask Activity, and this UI-only follow-up does not claim a fresh chain read or pending-switch pass. A separately approved retry first requires the site-connected B account on Studionet.

### Fourth attempt: pending-account recovery passes

The [fourth pending window](evidence/human-wallet/20260913-fourth-pending-account-window.json) captures Wallet A as visitor before and after an independent COMMITTING/pending receipt for B's exact transaction. The before-DOM request completes at `2026-09-13T07:41:22.149Z`; the receipt request starts at `07:41:23.863Z`, completes at `07:41:24.744Z`, and the after-DOM request starts at `07:41:24.909Z`. Both snapshots retain B as recovery signer and deny the valid public draft. The exact calldata/export, zero attached value, contract and sender are verified; the same hash is used throughout. The 115 sampling observations are preserved losslessly as timestamps plus indices into four distinct DOM snapshots, including the bounded-cycle gaps. No native accountsChanged-event timestamp is claimed.

The [final reconciliation](evidence/human-wallet/20260913-fourth-identity-reconciled.json) passes eight verification groups. Hash `0x6f8bccb77b08ca2ace92e78a858e84fae002c16e11e0324fa7b619dc187cdfec` finalizes successfully and binds UUID `db7e8ae8-a091-4b8c-9b1e-79a625733655` to review `d39b27e554c4f25aa8ac6ebf3a4ca74d63204659bbc967d4e03262a16ea940c0`. Strict successful receipt/call verification precedes deployed-source/protocol and complete stored-state reads. The full final leader payload equals the stored review, with `MATCHES_REFERENCES / ANSWERED / CLAIM_SUPPORTED`, the literal 14-day reference quote and raw chain `recorded_at` of `2026-09-13T07:41:09.548835+00:00`. Exactly one review increases twelve to thirteen; every earlier full review/timestamp, both reference versions/cards, roles/invitations and workspace settings remain unchanged.

After saved evidence replay, the [browser close](evidence/human-wallet/20260913-fourth-identity-browser-close.json) verifies four checks across six observations: terminal acknowledgment, cleared recovery under A, loading the older page to expose all thirteen UTC timestamps, and opening the new review/assessment without the unrelated invalid-ID alert. No new request remains. The agent clicked Continue to wallet but did not check the consent box: the before-action DOM already shows it checked, and the helper correctly recorded `agent_checked_app_consent=false`. An earlier commentary/general-note assumption and the first offline replay incorrectly expected an agent checkbox click; the closing record preserves that discrepancy, and the replay now requires the observed false value and checked DOM. Original receipts, timestamps, actor Booleans, source pins and earlier evidence JSON remain unchanged. No agent MetaMask approval or signature occurred.

```sh
node evidence/human-wallet/replays/20260913-fourth-identity-attempt.mjs
```

This is an offline replay of the scoped pending-account recovery and final single-review outcome, not a new transaction. Application source and the prior 237-test checkpoint are unchanged; the full suite is not rerun for evidence/documentation-only additions. Pending-network switching, actual concurrent sends, broader accessibility, public CI/immutable-commit evidence and release gates remain separate. New September 13 replays still require inclusion in the later public-workflow checkpoint. The earlier inconclusive and human-cancelled attempts are not relabeled as passes.

## September 12 browser/usability audit

The original [UI audit](UI-AUDIT.md) records 55 snapshots and 53 checks: 49 passed and four issues were identified. All five main screens fit at 320, 390, 768 and 1280 CSS-pixel widths; keyboard focus/activation, draft constraints, reference-version browsing, copy/export and opening the copied local review link were exercised. A new export exactly matches the earlier verified full review and reference bundle. The pass identified short-landscape dialog clipping, a late lookup overriding newer navigation, stale/read-inappropriate error feedback and missing answer-title limit guidance. No application or contract source was changed during that audit and no wallet transaction was requested or approved.

The [saved observations](evidence/browser/20260912-usability-audit.json) distinguish manual keyboard tab activation, transient test-input clearing and actual measured viewport sizes from attempted sizes. The browser was restored to its default viewport and existing ten-review history without dialogs, warnings, recovery metadata or leftover test drafts. Other browser engines, physical mobile/assistive-technology testing and public signed-out access remain unverified. Run `node evidence/browser/replay-20260912-usability.mjs --historical-ui` for an offline consistency check of the original evidence; it preserves the four original failures and does not rerun Chrome. The explicit flag checks the changed UI files against [preserved source snapshots](evidence/source-snapshots/README.md), not the new build.

### UI-fix follow-up

UI-01–UI-04 are implemented. The complete local suites pass 107 frontend tests (13 new regressions) and 97 contract/tooling tests; typecheck, lint, formatting and production build pass. Deferred reads exercise the actual component handlers, not a duplicate implementation. Byte-boundary tests include ASCII, accented text, emoji and trimming. Contract, wallet-signing hook, chain/receipt code, deployment and locks are unchanged. The [post-fix record](evidence/browser/20260912-ui-fix-verification.json) keeps this separate from the original browser evidence.

The user then approved reopening Chrome. The [fresh scoped browser retest](evidence/browser/20260912-ui-fix-browser-retest.json) passed 20 checks across 35 preserved observations, closing UI-01–UI-04. It covers 740×320/320×640 containment, keyboard scrolling/trapping/fixed-size focus return, manual/direct-link navigation and error retry, and ASCII/accent/emoji/empty/trimmed title boundaries. No publication or wallet request was initiated. The final copied review URL opens the verified existing v2 assessment with ten history entries, empty drafts, no dialog/recovery UI and the original viewport restored.

Run `node evidence/browser/replay-20260912-ui-fix-browser.mjs --historical-ui` for an offline consistency replay of that checkpoint. It checks its immutable hash, exact historical UI snapshot, other current source pins and preserved original audit/automated checkpoint, not a new browser session. Initial hydration, closing animation and two selector-wait deadlines are kept distinct from settled results. Resizing while a dialog was open returned focus to a potentially offscreen trigger; that original observation remains intact, with a separate fix and retest below. The existing owner session was reused, and no new chain-state/source RPC audit or automated suite rerun is implied by that earlier focused browser pass.

### Resize-focus and reference-gap wording follow-up

The answer-card dialog now scrolls its restored trigger into view after closing only when that same connected, enabled, rendered element actually has focus. It does not call focus itself, change the installed primitive, or redirect focus from newer navigation/dialogs. Unsupported claims absent from references get an explicitly labeled local rewrite stating the limitation; the original recorded suggestion remains expandable. Copying the original reply and exporting JSON preserve their exact input values, covered by actual-component rendering tests. This is not a rewrite of stored evidence or a new model verdict.

The complete local suite passed **120 frontend and 97 contract/tooling tests (217 total)**, including 13 additional focus/wording regressions. Typecheck, lint, formatting and production build passed. The Sites helper failed on a Windows npm launcher path; the unchanged pinned `npm run build` fallback succeeded. No dependencies were installed or upgraded in this pass.

After rebuilding/restarting the same local preview, **12 Chrome checks across 19 observations** passed: resize-before-Escape, portrait-to-landscape Close, fixed-size focus return, keyboard trapping/internal scrolling, the actual historical mixed-claim rewrite/disclosure and mobile overflow. The original matching v2 review, ten-review history, empty drafts, no dialog/alerts and original viewport were restored. No wallet request or transaction was initiated.

Evidence: [focus and wording verification](evidence/browser/20260912-focus-wording-verification.json). Replay it with `node evidence/browser/replay-20260912-focus-wording.mjs --historical-ui`. This validates saved observations and exact historical frontend snapshots, with other hashes still checked against current source; it does not run a new browser test. Original audit failures, the previous offscreen-focus observation and the original “processing time may vary” output remain hash-preserved. Other engines/devices/assistive technology, broader live reliability cases, approved GitHub/hosting, public Ubuntu CI, signed-out evaluator access and submission approval remain open.

### Local reliability and accessibility follow-up

Twenty additional regressions reproduced and then closed two issues: stale wallet-state failure feedback after newer successful hydration, and infinite reduced-motion animation loops. The complete suite passed **140 frontend and 97 contract/tooling tests**, with typecheck, lint and production build passing. The direct-tests skill was used for the local contract rerun; model responses there are mocked. No contract, deployment or dependency lock changed. The wallet-state hook's error guard changed, but exact AST comparisons show all seven send/check/persistence/recovery callbacks are unchanged.

Seventeen session tests execute the actual hook callbacks against isolated storage, lock, provider and receipt/state doubles. They cover same-tab duplicates, competing-tab lock semantics, stale-tab durable-intent checks, unavailable storage, post-response hash storage failure, explicit rejection, unknown outcomes, strict success/state checks, overlapping status reads, unmount, hash attachment, gated dismissal and out-of-order wallet hydration. These are not native multi-tab or human-wallet executions. Three accessibility source tests cover reduced motion, ten opaque foreground/background theme pairs and existing focus/forced-color rules.

The rebuilt Chrome preview passed **12 scoped checks across 16 observations**: mobile reflow/labels on all five main screens, keyboard tab navigation, unsigned consent/Cancel/reset, safe alert/native status semantics and restored clean history. The checkbox was toggled only inside a local unsigned dialog; **Continue to wallet was never activated**. The original ten-review state and empty drafts were restored. Native zoom shortcuts did not change measured dimensions or pixel ratio, so 200% enlargement is not claimed. Reduced-motion/forced-colors emulation and a live screen reader were unavailable in this browser surface and remain unverified; source checks are not substitutes.

Evidence: [local reliability/accessibility](evidence/browser/20260912-reliability-accessibility.json). Replay: `node evidence/browser/replay-20260912-reliability-accessibility.mjs`. The prepared Ubuntu workflow now also runs the four historical evidence replays and this current-scope replay. This workflow change is local: no GitHub repository, push, public Actions execution or hosting publication occurred.

Accessibility references: [W3C contrast](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html), [text enlargement](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html), and [motion](https://www.w3.org/WAI/WCAG22/Understanding/animation-from-interactions.html). Selected opaque theme pairs meet 4.5:1; this does not certify every rendered gradient/hover state or full WCAG conformance.

## September 13 release preparation and pending-network preflight

The complete existing Windows suites were rerun: 140 frontend/transport tests
and 97 contract/tooling tests passed, together with formatting, lint and types.
These runs used the existing locked environments; they are not a clean Ubuntu
run. After adding four offline-evidence reader/inventory regressions, the full
frontend suite was rerun and all 144 tests passed, bringing the current total
to 241. Formatting, lint and types also pass for the updated release tooling.

The [pending-network preparation](evidence/human-wallet/20260913-pending-network-preparation.json)
freshly verifies the previous successful transaction, exact calldata, final
leader return and deployed source before reading all ten complete public-state
outputs. All 13 reviews and their stored timestamps remain unchanged. The next
operation predeclares the public 14-day fixture and only a thirteen-to-fourteen
review delta. A fresh app-generated UUID, new hash and derived review ID remain
unknown until the human authorizes a new request. None of the earlier hashes is
eligible for resubmission.

The [unsigned setup](evidence/human-wallet/20260913-pending-network-unsigned-setup.json)
records B back as owner on Studionet and a fresh confirmation with unchecked
consent. It separately pins the current release-tooling files and records the
workflow-only transition after the read-only checkpoint; all existing
application, contract, deployment and dependency pins are unchanged. This is
preparation, not a pending-network pass. The required evidence is an independent
pending receipt bracketed by off-Studionet observations retaining B's original
recovery hash and denying a valid draft, followed by exact final reconciliation
and a safe Studionet return. Observation timestamps must not be substituted for
stored chain timestamps. A generic off-network label does not independently
prove the alternative chain ID.

The workflow now runs the full explicit offline inventory:

```sh
node scripts/verify-saved-evidence.mjs
```

All 18 registered replays pass locally. The inventory includes the newer
September 13 records, preserves inconclusive/cancelled outcomes as recorded,
and replays the completed automated Studionet evidence without sending RPCs or
transactions. The original workflow is archived byte-for-byte, with its SHA-256
checked through the explicit `--historical-workflow` option. This only changes
where the old workflow pin is read; it does not relax application or contract
pins. See [source snapshots](evidence/source-snapshots/README.md).

Public CI, live pending-network/concurrency/adversarial cases, broader native
accessibility and release approval remain open. No GitHub repository, push,
hosting publication or submission was performed by this preparation.

## September 13 recovery-hash correction

The pending-network attempt was stopped at its unsigned confirmation. No new
hash was observed. The agent cancelled only that app dialog and did not click
Continue or approve a wallet request. The saved
[pause record](evidence/human-wallet/20260913-pending-network-paused.json)
supersedes the earlier instruction to approve the staged test.

Source inspection and an isolated test reproduced a recovery defect: a
well-formed but unrelated hash was persisted before exact-call verification,
and the resulting pending UI had no correction path. The first 11 new
regressions had 10 failures; their original output is preserved. The fix checks
receipt identity, exact calldata and zero value before saving a candidate,
retains the old record on mismatch/missing receipt/network failure, and adds a
collapsed, labeled correction control for an existing incorrect hash. It sends
no new transaction. Shared Web Locks, durable-record comparison and unmount
guards protect both hash replacement and the human no-send acknowledgment.

Fourteen focused recovery tests now cover this behavior, including seven saved
public-receipt variants through the actual app receipt client with an isolated
RPC boundary. Five evidence-reader/inventory tests preserve explicit snapshot
selection. The complete local suite passes at 159 frontend/transport plus 97
contract/tooling tests, with type checks, lint, formatting, GenVM validation
and production build. A temporary npm audit DNS failure was retained; the
successful retry and Python audit reported no known vulnerabilities.

The separate [current-source fix proof](evidence/local/20260913-recovery-hash-fix.json) records the fresh successful previous
transaction and all ten unchanged full public-state outputs: exactly 13
reviews, the original stored timestamps, both reference versions/cards and
unchanged access state. The rebuilt preview receives only a read-only smoke
check; actual native-wallet hash correction and pending-network switching
remain unexercised on this new build.

Old proofs use explicit byte-pinned historical UI/workflow snapshots. They are
not silently applied to the changed frontend. The current fix replay requires
current source pins without historical flags. No repository creation, push,
public hosting, public Ubuntu pass or submission approval is claimed. The
offline inventory now contains 19 replays, including this current-source proof:

```sh
node evidence/browser/replay-20260913-recovery-hash-fix.mjs
node scripts/verify-saved-evidence.mjs
```

## Optional WebMCP

`replycheck_read_context` exposes public selection state without draft text. `replycheck_stage_draft` stages content for human review; it cannot sign or publish. Existing draft replacement is explicit. Registration uses abort cleanup.

Unit tests exercise registration contracts and state transitions through a stub registry. A supported browser-native `document.modelContext` session has **not** been validated. Do not label those tools browser-verified until that focused check is completed.
